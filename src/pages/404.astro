---
import Layout from '../layouts/Layout.astro';
import { toolRegistry } from '../data/tool-registry';
---

<Layout
  title="Page Not Found"
  description="The page you are looking for does not exist. Browse our free online image tools."
>
  <section style="text-align: center; padding: 3rem 0;">
    <h1>Page Not Found</h1>
    <p style="color: var(--color-text-light); margin: 1rem 0 2rem;">
      The page you're looking for doesn't exist or has been moved.
    </p>
    <div class="related-tools-grid" data-tool-directory="not-found">
      {
        toolRegistry.map((tool) => (
          <a href={tool.href} class="tool-card" data-tool-id={tool.id}>
            <h3>{tool.name}</h3>
            <p>{tool.shortDescription}</p>
          </a>
        ))
      }
    </div>
  </section>
</Layout>
